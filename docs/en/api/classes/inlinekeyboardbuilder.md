---
description: Builder for conveniently constructing an InlineKeyboardMarkup. Lets you set keyboard options and add buttons and rows dynamically.
---

# InlineKeyboardBuilder

Builder for conveniently constructing an InlineKeyboardMarkup. Lets you set keyboard options and add buttons and rows dynamically.

Inherits `KeyboardBuilderBase<IInlineContent`, `InlineKeyboardMarkup`, `InlineKeyboardBuilder>`.

## Methods

| Method | Description |
| --- | --- |
| `override InlineKeyboardMarkup Build()` |  |

